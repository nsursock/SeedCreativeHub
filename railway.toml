# Railway service config for the Fastify API (linked to GitHub main).
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile.railway"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
