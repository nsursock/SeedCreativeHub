# Railway service config for the Fastify API (link this service before `railway up`).
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
# Healthcheck after listen; keep generous timeout for cold starts.
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
