# Railway service config for the Fastify API (linked to GitHub main).
[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
# Temporarily omit healthcheckPath — recent deploys fail after image push while
# the previous SUCCESS revision keeps serving. Re-enable once stable.
restartPolicyType = "ON_FAILURE"
